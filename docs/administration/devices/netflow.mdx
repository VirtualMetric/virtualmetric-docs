# NetFlow

## Synopsis

```yaml
- id: <numeric>
  name: <string>
  description: <string>
  type: netflow
  tags: <string[]>
  pipelines: <pipeline[]>
  status: <boolean>
  properties:
    address: <string>
    port: <numeric>
    reuse: <boolean>
    buffer_size: <numeric>
    stats_frequency: <numeric>
```

## Description

Creates a NetFlow v5 collector that accepts flow data over UDP connections. Supports high-performance collection with multiple worker processes and configurable buffer sizes. Note that for NetFlow v9 or higher versions, use the `ipfix` collector type instead.

## Basic Configuration

The following are the minimum requirements to define the device.

### Device

|Field|Required|Default|Description|
|---|---|---|---|
|`id`|Y||Unique identifier|
|`name`|Y||Device name|
|`description`|N|-|Optional description|
|`type`|Y||Must be `netflow`|
|`tags`|N|-|Optional tags|
|`pipelines`|N|-|Optional pre-processor pipelines|
|`status`|N|`true`|Enable/disable the device|

### Network

|Field|Required|Default|Description|
|---|---|---|---|
|`address`|N|`"0.0.0.0"`|Listen address|
|`port`|N|`2055`|Listen port|
|`reuse`|N|`true`|Enable socket address reuse|

### Performance

|Field|Required|Default|Description|
|---|---|---|---|
|`buffer_size`|N|`9000`|Network read buffer size in bytes|
|`stats_frequency`|N|`300`|Statistics collection interval in seconds|

## Advanced Features

### Multi-Worker Use

When `reuse` is enabled, the collector automatically scales to use multiple worker processes based on available CPU cores. Each worker:
- Maintains its own UDP listener
- Processes flows independently
- Writes to a dedicated queue file

### Flows

The collector supports:
- Fixed format NetFlow v5 records
- Application identification
- Port-based protocol mapping
- Flow state tracking
- Statistics aggregation

## Examples

### Basic Configuration

The minimum required configuration using defaults:

<ExampleGrid>
  <CommentCol>
    Create a simple NetFlow collector...
  </CommentCol>
  <CodeCol>
    ```yaml
    - id: 1
      name: basic_netflow
      type: netflow
      properties:
        port: 2055
    ```
  </CodeCol>
</ExampleGrid>

### High-Performance Configuration

For enhanced performance with high flow volumes:

<ExampleGrid>
  <CommentCol>
    Optimized for high message volumes...
  </CommentCol>
  <CodeCol>
    ```yaml
    - id: 2
      name: performance_netflow
      type: netflow
      properties:
        address: "0.0.0.0"
        port: 2055
        reuse: true
        buffer_size: 32768
    ```
  </CodeCol>
</ExampleGrid>

:::note
When `reuse` is enabled, the collector automatically scales to use all available CPU cores for processing.
:::

### Legacy Network Configuration

For collecting from older network devices:

<ExampleGrid>
  <CommentCol>
    Collect from legacy network devices...
  </CommentCol>
  <CodeCol>
    ```yaml
    - id: 3
      name: legacy_netflow
      type: netflow
      properties:
        address: "0.0.0.0"
        port: 2055
        reuse: true
        buffer_size: 16384
        stats_frequency: 60
    ```
  </CodeCol>
</ExampleGrid>

:::warning
This collector only supports NetFlow v5. For NetFlow v9 or IPFIX, use the `ipfix` collector type.
:::

### Application Protocol Identification

The collector supports application protocol identification using a variety of methods:

1. Application-based identification using app-definitions.csv
2. TCP port mapping using tcp-definitions.csv
3. UDP port mapping using udp-definitions.csv

The definition files can be placed in three locations, in the following order:

1. `/user/definitions/app-definitions-{device-id}.csv` (device-specific)
2. `/user/definitions/app-definitions.csv` (user-defined)
3. `/package/definitions/app-definitions.csv` (system defaults)

:::warning
Definition files must be in CSV format with exactly two columns per row.
:::

<ExampleGrid>
  <CommentCol>
    Basic flow collection with application identification...
  </CommentCol>
  <CodeCol>
    ```yaml
    - id: 4
      name: app_aware_netflow
      type: netflow
      properties:
        port: 2055
        reuse: true
        buffer_size: 16384
    ```
  </CodeCol>
</ExampleGrid>

Definition files use the CSV format:

`app-definitions.csv`:

```csv
SSH,Secure Shell
RDP,Remote Desktop
HTTP,Web Browsing
HTTPS,Secure Web
```

`tcp-definitions.csv`:

```csv
22,SSH
3389,RDP
80,HTTP
443,HTTPS
```

`udp-definitions.csv`:

```csv
53,DNS
67,DHCP
123,NTP
161,SNMP
```

:::note
The collector will fall back to system defaults if custom definition files are not found.
:::

### Version Compatibility

|NetFlow Version|Supported|Alternative|
|---|---|---|
|v1|No|-|
|v2|No|-|
|v3|No|-|
|v4|No|-|
|v5|Yes|-|
|v6|No|-|
|v7|No|-|
|v8|No|-|
|v9|No|Use `ipfix` type|
|v10 (IPFIX)|No|Use `ipfix` type|

:::warning
For NetFlow v9 or IPFIX support, configure your collector for the `ipfix` type instead of the `netflow` type.
:::
