---
sidebar_label: Basics
---

# Getting Started: Basics

**VirtualMetric Director** telemetry processes are configured with YAML format files. In this chapter, we will give you a brief summary of how these files are used and the minimum configurations required.

## The Files

For our purposes, we will put the files in the examples under the `config` directory of `<vm_root>`, i.e. the directory where you have placed **Director**.

:::note
To have a basic understanding of what types of files used, where they are placed, and how they can be arranged, read [**this section**](/docs/configuration/index.mdx#files-and-directories).
:::

## The CLI

You can invoke the processes you want **Director** to run from a terminal. The `-help` switch will enlist the available parameters.

Here are a few typical use cases:

* Collecting _Syslog_:

  <Tabs groupId="os-options">
    <TabItem value="windows" label="Windows" default>
      ```powershell
      vmetric-director -mode=generator -protocol=syslog -address=127.0.0.1:514
      ```
    </TabItem>
    
    <TabItem value="linux" label="Linux" default>
      ```bash
        vmetric-director -mode=generator -protocol=syslog -address=127.0.0.1:514
      ```
    </TabItem>

    <TabItem value="macos" label="macOS" default>
      ```bash
        vmetric-director -mode=generator -protocol=syslog -address=127.0.0.1:514
      ```
    </TabItem>
  </Tabs>

* Viewing logs with a specific debug level:

  <Tabs groupId="os-options">
    <TabItem value="windows" label="Windows" default>
      ```powershell
      vmetric-director -mode=console -debugLevel=3
      ```
    </TabItem>
    
    <TabItem value="linux" label="Linux" default>
      ```bash
        vmetric-director -mode=console -debugLevel=3
      ```
    </TabItem>

    <TabItem value="macos" label="macOS" default>
      ```bash
        vmetric-director -mode=console -debugLevel=3
      ```
    </TabItem>
  </Tabs>

* Running a specific pipeline with validation:

  <Tabs groupId="os-options">
    <TabItem value="windows" label="Windows" default>
      ```powershell
      vmetric-director -mode=pipeline -name=<pipeline_name> -validate
      ```
    </TabItem>
    
    <TabItem value="linux" label="Linux" default>
      ```bash
        vmetric-director -mode=pipeline -name=<pipeline_name> -validate
      ```
    </TabItem>

    <TabItem value="macos" label="macOS" default>
      ```bash
        vmetric-director -mode=pipeline -name=<pipeline_name> -validate
      ```
    </TabItem>
  </Tabs>

We will provide the relevant commands to execute the configured examples.

## The Components

To stream and collect data points, to process them, and to forward them to destinations, **Director** uses the following types of configurable components:

* **Devices** - Data sources that collect or receive data. See its [properties](/docs/configuration/devices/index.mdx#properties).

* **Targets** - Destinations where processed data is sent. See its [properties](/docs/configuration/targets/index.mdx#properties).

* **Pipelines** - String of processors that transform the data. See its [properties](/docs/configuration/pipelines/index.mdx#properties).

* **Routes** - Predefined data flows from devices through pipelines to targets. See its [properties](/docs/configuration/routes.mdx#properties).

## The Examples

After reviewing the indicated sections, you can see **Director** in action by trying the following introductory examples in the indicated order:

1. [Example: Ingesting Data](/docs/getting-started/example-device.mdx)

2. [Example: Forwarding Data](/docs/getting-started/example-target.mdx)

3. [Example: Processing Data](/docs/getting-started/example-process.mdx)

4. [Example: Routing Data](/docs/getting-started/example-route.mdx)

5. [Example: A Syslog-To-Sentinel Pipeline](/docs/getting-started/example-pipeline.mdx)
