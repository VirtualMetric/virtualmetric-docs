import {Synopsis, Example, Description, SampleCode} from "@site/src/components/CustomFeatures";

# Remove

<Synopsis>
Removes existing fields. If a field doesn’t exist, an exception is raised.
</Synopsis>

## Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`field`|Y|N/A|List of fields to remove|
|`description`|N|-|Explanatory note|
|`if`|N|-|Condition to run|
|`ignore_failure`|N|`false`|See [Handling Failures](../handling-failures.mdx)|
|`ignore_missing`|N|`false`|If `true` and `field` does not exist or is `null`, exit quietly without making any modifications|
|`on_failure`|N|-|See [Handling Failures](../handling-failures.mdx)|
|`tag`|N|-|Identifier|

## Examples

<Example>
   <Description>
      We have two fields: _foo_ and _bar_.
   </Description>
   <SampleCode>
      ```js
      {
         "field": ["foo", "bar"]
      }
      ```
   </SampleCode>
   <Description>
      We want to remove _foo_.
   </Description>
   <SampleCode>
      ```js
      {
         "remove": {"field": "foo"}
      }
      ```
   </SampleCode>
   <Description>
      Done.
   </Description>
   <SampleCode>
      ```js
      {
         "field": ["bar"]
      }
      ```
   </SampleCode>
</Example>
