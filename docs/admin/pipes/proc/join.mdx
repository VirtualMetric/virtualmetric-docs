# Join

:::info[synopsis]
Joins the elements of an array in a single string, using a separator character between them. Raises an exception if the field does not contain an array.
:::

|Field|Type|Required|Default|Description|
|---|---|---|---|---|
|`field`|String|Y|N/A|Field containing the array values to join|
|`separator`|String|Y|-|Separator character|
|`description`|String|N|-|Explanatory note|
|`if`|String|N|-|Execute on this condition|
|`ignore_failure`|Logical|N|`false`|See [Handling Failures](../handling-failures.mdx)|
|`ignore_missing`|Logical|N|`false`|If set to `true` and `field` does not exist or is `null`, exit quietly without making any modifications|
|`on_failure`|Processors|N|-|See [Handling Failures](../handling-failures.mdx)|
|`on_success`|Processors|N|-||
|`tag`|String|N|-|Identifier|
|`target_field`|String|N|-|The field to assigned the string containing the joined elements to|

:::note[examples]
**Spec**:

```json
{
   "join": {
      "field": "sales_regions",
      "separator": ","
   }
}
```
:::
