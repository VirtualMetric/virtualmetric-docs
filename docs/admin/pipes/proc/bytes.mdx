import Synopsis from "@site/src/components/Synopsis";

# Bytes

<Synopsis>
Converts a string expressing size written in byte units to its value in bytes, e.g. `1KB` to `1024`.
</Synopsis>

If the field contains multiple values, all are converted. Allowed units are `B`, `KB`, `MB`, `GB`, `TB`, and `PB`, all case insensitive. If the string is not in one of the enumerated formats or exceeds $2^{64}-1$ in value, an error is raised.

## Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`field`|Y|N/A|The filed containing the unit values|
|`description`|N|-|Explanatory note|
|`if`|N|-|Execute on this condition|
|`ignore_missing`|N|`false``|If `true` and `field` does not exist or contains no value, exit quietly without making any modifications|
|`ignore_failure`|N|-||
|`on_failure`|N|-|See [Handling Failures](../handling-failures.mdx)|
|`on_success`|N|-|Use to inform the user that no errors were raised or to specify the next processor to run|
|`tag`|N|-|Identifier|
|`target_field`|N|-|The field to assign the converted value to, if distinct from `field`|

## Examples

```js
{
   "bytes": {
	   "field": "file.size"
   }
}
```
