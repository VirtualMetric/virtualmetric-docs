# Redis

## Synopsis

```yaml {5}
devices:
  - id: <numeric>
    name: <ip-numeric>
    description: <text>
    type: redis
    status: <logical>
    properties:
      address: <ip-string>
      port: <numeric>
      username: <string>
      password: <string>
      channel: <string>
      tls:
        status: <logical>
        cert_name: <string>
        key_name: <string>
```

## Description

Represents the listener streaming data from the Redis protocol.

## Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`id`|Y|N/A|Device identifier|
|`name`|Y|N/A|Address of the listener, e.g. `127.0.0.1`|
|`description`|N|-|Explanatory note|
|`type`|Y|N/A|Type of device. Should be `redis`|
|`status`|N|`true`|Enable device|

### `properties:`

|Field|Required|Default|Description|
|---|---|---|---|
|`address`|N|`"0.0.0.0"`|Listening address|
|`port`|N|`0`|Listening port|
|`username`|N|`""`|Required if authentication is used|
|`password`|N|`""`|Required if authentication is used|
|`channel`||`""`||

### `tls:`

|Field|Required|Default|Description|
|---|---|---|---|
|`status`|N|`false`|Enable TLS connection|
|`cert_name`|N|`"cert.pem"`|TLS certificate file path|
|`key_name`||`"key.pem"`|TLS key file path|

## Examples

<details>
<summary>Sample code</summary>
```yaml
```
</details>

{/* TODO: Complete */}