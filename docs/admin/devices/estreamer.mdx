# EStreamer

## Synopsis

```yaml
devices:
  - id: <numeric>
    name: <ip-numeric>
    description: <text>
    type: estreamer
    status: <logical>
    properties:
      address: <ip-string>
      port: 8302
      tls:
        non_secure: <logical>
      cert_name: <string>
      key_name: <string>
      reuse: <logical>
      flush_interval: <numeric>
      queue:
        interval: <numeric>
      batch_size: <numeric>
      inputs:
        (- id: <numeric>
        name: <string>
        status: <logical>)+
```

## Description

### Protocol Content

### Common Settings

<dl>
  <dt>**Transport**</dt>
  <dd>Port **8302**</dd>
</dl>

## Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`id`|Y|||
|`name`|Y|`127.0.0.1`||
|`description`|N|-|Explanatory note|
|`type`|Y|`estreamer`||
|`status`||`false`||

## `properties` Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`address`|Y|"0.0.0.0"||
|`port`|Y|8302||
|`tls:non_secure`||`false`||
|`cert_name`||`""`||
|`key_name`||`""`||
|`reuse`||`false`||
|`flush_interval`||`1`||
|`queue:interval`||`1`||
|`batch_size`||`1000`||
|`inputs:id`||||
|`inputs:name`||||
|`inputs:status`||||

## Use Cases

<details>
<summary>Sample code</summary>
```yaml
```
</details>

{/* TODO: Complete */}