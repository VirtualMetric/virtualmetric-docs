# Kafka

## Synopsis

```yaml
devices:
  - id: <numeric>
    name: <ip-numeric>
    description: <text>
    type: kafka
    status: <logical>
    properties:
      address: <ip-string>
      port: 0
      username: <string>
      password: <string>
      partitions: <numeric>
      group: <string>
      balancer: <enum-string>
      topic: <string>
      algorithm: <enum-string>
      tls:
        status: <logical>
        cert_name: <string>
        key_name: <string>
```

## Description

Represents the Kafta protocol.

Kafka is a distributed event streaming platform designed for high-throughput, fault-tolerant, and durable data pipelines. It is ideal for use cases such as real-time analytics, event sourcing, and stream processing, leveraging a publish-subscribe messaging model.

## Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`description`|N|-||
|`id`|Y|||
|`name`|`Y|`127.0.0.1`||
|`status`|Y|`false`||
|`type`|Y|`kafka`||
|`properties.address`|Y|`"0.0.0.0"`||
|`properties.algorithm`|N|`""`|Valid options: `"none"`, `"plain"`, `"scram-sha-256"`, `"scram-sha-512"`|
|`properties.balancer`|N|`""`|Valid options: `"roundrobin"`, `"cooperativesticky"`, `"range"`, `"sticky"`|
|`properties.group`|N|`""`||
|`properties.partition`|N|`1`||
|`properties.password`|N|`""`||
|`properties.port`|Y|`0`||
|`properties.tls.cert_name`|N|`""`||
|`properties.tls.key_name`|N|`""`||
|`properties.tls.status`|N|`true`||
|`properties.topic`|N|`""`||
|`properties.username`|N|`""`||

## Examples

<details>
<summary>Sample code</summary>
```yaml
```
</details>

## Protocol Content

{/* TODO: Complete */}