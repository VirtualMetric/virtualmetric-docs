# Post-processing

This is the stage where the pipelines attached to a destination do what is known as [normalization](./normalization.mdx) before the data is sent. Here, the processors are applied to all data points. Typically, these are processors that re-shape or aggregate the data points as per the receiver side's requirements.

We offer unique features to handle the following operations:

* **Log routing**: routing log data to multiple destinations

{/* TODO: Complete */}
