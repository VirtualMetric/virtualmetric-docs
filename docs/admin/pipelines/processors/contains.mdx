# Contains

## Synopsis

```yaml {2}
contains:
  - field: <ident>
  - description: <text>
  - if: <script>
  - ignore_failure: <logical>
  - ignore_missing: <logical>
  - list: <strings...>
  - list_field: <ident>
  - on_failure: <processors...>
  - on_success: <processors...>
  - tag: <string>
```

## Description

**Functionality**: Checks the existence of a value in a list of values.

## Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`field`|Y|N/A|Field that contains the value to check|
|`description`|N|-|Explanatory note|
|`if`|N|-|Condition to run|
|`ignore_failure`|N|`false`|See [Handling Failures](../handling-failures.mdx)|
|`ignore_missing`|N|`false`|If `true` and `field` does not exist or is `null`, exit quietly without making any modifications|
|`list`|N|-|List of values to check|
|`list_field`|N|-|Field containing the list of values to check|
|`on_failure`|N|-|See [Handling Failures](../handling-failures.mdx)|
|`on_success`|N|-|See [Handling Success](../handling-success.mdx)|
|`tag`|N|-|Identifier|

## Examples

<ExampleGrid>
  <CommentCol>
    The field contains 3 items:
  </CommentCol>
  <CodeCol>
    ```json
    {
      "data": ["foo", "bar", "baz"]
    }
    ```
  </CodeCol>
  <CommentCol>
    The value _foo_ is among them:
  </CommentCol>
  <CodeCol>
    ```yaml
    contains:
      - field: "foo"
      - list: data
    ```
  </CodeCol>

  <CommentCol>
    The value _qux_ is _not_ among them:
  </CommentCol>
  <CodeCol>
    ```yaml
    contains:
      - field: "qux"
      - list: data
    ```
  </CodeCol>
</ExampleGrid>
