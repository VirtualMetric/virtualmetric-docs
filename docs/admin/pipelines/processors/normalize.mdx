# Normalize

<Synopsis>
Converts the source type fields to the target type fields such as [**ECS**](../../../reference//ecs.mdx) to [**CIM**](../../../reference/cim.mdx) or [**ASIM**](../../../reference/asim.mdx), or vice versa.
</Synopsis>

## Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`description`|N|-|Explanatory note|
|`if`|N|-|Condition to run|
|`ignore_failure`|N|`false`|See [Handling Failures](../handling-failures.mdx)|
|`on_failure`|N|-|See [Handling Failures](../handling-failures.mdx)|
|`on_success`|N|-|See [Handling Success](../handling-success.mdx)|
|`source_format`|N|-|Field containing the source type|
|`tag`|N|-|Identifier|
|`target_format`|N|-|Field to assign converted type|

## Examples

Normalize the data in field **foo** by converting it to the **ASIM** format:

```json
{
   "processors": [
      "normalize"
      {
         "source": "foo",
         "target_format": "ASIM"
      }
   ]
}
```
