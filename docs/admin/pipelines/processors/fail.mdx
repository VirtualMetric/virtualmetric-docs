# Fail

<Synopsis>
Raises an exception.
</Synopsis>

Useful to display a specific message to the requester when a pipeline is expected to fail.

## Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`message`|Y|N/A|Error message displayed|
|`description`|N|-|Explanatory note|
|`if`|N|-|Condition to run|
|`ignore_failure`|N|`false`|See [Handling Failures](../handling-failures.mdx)|
|`on_failure`|N|-|See [Handling Failures](../handling-failures.mdx)|
|`on_success`|N|||
|`tag`|N|-|Identifier|

## Examples

```json
{
   "fail": {
      "if" : "doc.tags.contains('foo')",
      "message": "Foo tag not present; available tags: {{{tags}}}"
   }
}
```
