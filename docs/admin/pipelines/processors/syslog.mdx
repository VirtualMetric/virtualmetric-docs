# Syslog

## Synopsis

```yaml {2}
syslog:
   - field: <ident>
   - description: <string>
   - if: <script>
   - ignore_failure: <logical>
   - ignore_missing: <logical>
   - on_failure: <processors...>
   - on_success: <processors...>
   - tag: <string>
   - target_field: <ident>
```

## Description

**Functionality**: Converts a [**Syslog**](../../../reference/syslog.mdx) message into a structured _syslog_ object.

## Parameters

|Field|Required|Default|Description|
|---|---|---|---|
|`field`|Y|N/A|Field containing the **Syslog** message|
|`description`|N|-|Explanatory note|
|`if`|N|-|Condition to run|
|`ignore_failure`|N|`false`|See [Handling Failures](../handling-failures.mdx)|
|`ignore_missing`|N|`false`|If set to `true` and `field` does not exist or is `null`, exit quietly without making any modifications|
|`on_failure`|N|-|See [Handling Failures](../handling-failures.mdx)|
|`on_success`|N|-|See [Handling Success](../handling-success.mdx)|
|`tag`|N|-|Identifier|
|`target_field`|N|-|Field to assign the _syslog_ object|

## Examples

{/* TODO: Verify the example below */}

```yaml {3}
  timestamp: "2024-05-15T22:12:20.015000"
  hostname: "example.com"
  app_name: "foo-log"
  process_id: "288"
  message_id: "155"
  structured_data:
      - eventSource: "Application"
      - eventID: 850
  message: "Foo log entry"
```
