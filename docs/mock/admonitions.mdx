---
pagination_prev: mock/README
pagination_next: null
---

# Admonitions

The following is a custom `info` admonition.

<Synopsis>
You can **format** this admonition anyway you like.

You can have paragraphs, too. You can even embed code segments in it:

```Python
def func(n: int) -> None:
	print(f"{n}")
```
</Synopsis>
