import DirectorsFull from '../assets/gui/directors-full.png';
import DirectorsCreateFull from '../assets/gui/directors-create-full.png';

# Directors Screen

This is the working area where you manage your **Director**s.

<img src={DirectorsFull} style={{maxWidth: "100%"}} />

To create a new **Director**, click the **Create Director** button on the top right. You will be taken to the following form.

<img src={DirectorsCreateFull} style={{maxWidth: "100%"}} />
