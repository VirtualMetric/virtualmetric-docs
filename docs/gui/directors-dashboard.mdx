import DirectorsFull from '../assets/gui/directors-full.png';
import DirectorsCreateFull from '../assets/gui/directors-create-full.png';
import CreateDirectorFull from '../assets/gui/create-director-full.png';
import CreateUserFull from '../assets/gui/create-user-full.png';

# Directors Dashboard

This is the working area where you manage your **Director**s.

<img src={DirectorsFull} style={{maxWidth: "100%"}} />

To create a new **Director**, click the **Create Director** button on the top right. You will be taken to the following form.

<img src={DirectorsCreateFull} style={{maxWidth: "100%"}} />

## Additional Director Management Views

Here are additional views for managing directors:

<img src={CreateDirectorFull} style={{maxWidth: "100%"}} alt="Create director full form"/>

You can also manage users associated with directors:

<img src={CreateUserFull} style={{maxWidth: "100%"}} alt="Create user full form"/>
