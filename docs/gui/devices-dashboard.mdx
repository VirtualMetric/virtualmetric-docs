import DevicesFull from '../assets/gui/devices-full.png';
import DevicesSyslogFull from '../assets/gui/devices-syslog-full.png';
import EstreamerFull from '../assets/gui/estreamer-full.png';

# Devices Dashboard

This is the working area where you manage your devices.

<img src={DevicesFull} style={{maxWidth: "100%"}} />

**Example** - A _Syslog_ device

<img src={DevicesSyslogFull} style={{maxWidth: "100%"}} />

**Example** - An _Estreamer_ device

<img src={EstreamerFull} style={{maxWidth: "100%"}} alt="Estreamer device full view"/>
