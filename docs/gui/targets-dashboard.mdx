import TargetsFull from '../assets/gui/targets-full.png';
import TargetsConsoleFull from '../assets/gui/targets-console-full.png';

# Targets Dashboard

This is the working area where you manage your targets.

<img src={TargetsFull} style={{maxWidth: "100%"}} />

**Example** - A _Console_ target:

<img src={TargetsConsoleFull} style={{maxWidth: "100%"}} />
