:::caution
Deleted child pipelines cannot be recovered. Before deleting, consider exporting the pipeline YAML for backup. See <Topic id="pipelines-overview-config">Pipeline configuration</Topic> for export options.
:::
