:::info
We need to set the component's `status` to `true` to enable it.

Just as **Director** allows you to add, modify, or remove devices and targets while it is running, by the same mechanism, it allows you to start or stop them by enabling or disabling them.
:::
