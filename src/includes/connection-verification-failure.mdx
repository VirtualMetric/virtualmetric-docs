:::note Attention
Connection verification may fail when

- the **Verify connection** button is clicked without an installed director, or before the installation completes
- the local machine is in an invalid state
- the firewall settings on the local machine prevent the connection

In these cases, the process will return an error message.
:::
