:::warning
Installed templates cannot be re-installed. If you attempt to install a template that is already installed:

- A modal window appears with an error message, explaining that the content is already installed
- The modal provides information about the existing installation
- You must click **OK** or **Cancel** to close the error modal

The system prevents duplicate installations to maintain content integrity
:::
