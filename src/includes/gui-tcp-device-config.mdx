<Tabs>
  <TabItem value="protocol" label="Protocol Settings" default>
    #### Device Protocol Settings

    - **IP Address** (Optional) - Listening address for the TCP device. By default, `0.0.0.0`
    - **Port** - Listening port for the TCP device. By default, `6003`

    <Include id="port-open-for-inbound-traffic" />

    #### TCP Settings

    - **Framing** (Optional) - Framing mode for TCP. By default, `delimiter`
    - **Line Delimiter** (Optional) - Character used to delineate text lines for TCP framing. By default, `\n`
    - **Max Connections** (Optional) - Maximum concurrent TCP connections. By default, `1000`
    - **Max Message Size** (Optional) - Maximum message size in bytes. By default, `20971520`
    - **Timeout** (Optional) - Connection timeout in seconds. By default, `300`

    #### TLS Settings

    - **TLS Encryption** - Toggle to enable/disable TLS encryption. By default, disabled

    Click **Back** to return to the previous step, click **Next step** to continue.
  </TabItem>
  <TabItem value="advanced" label="Advanced Configuration" default>
    #### Performance

    - **Socket address reuse** - Toggle to enable/disable socket reuse. By default, enabled
    - **Workers** (Optional) - Number of worker processes spawned when reuse is enabled. By default `0` for max workers
    - **Buffer Size** (Optional) - Network read buffer size in bytes. By default, `1048576`

    #### Messages

    - **Batch Size** (Optional) - Number of messages to batch before flushing. By default, `1000`
    - **Queue Interval** (Optional) - Queue processing interval in seconds. By default, `1`
  </TabItem>
</Tabs>

---

:::note
You can find further details of the TCP device <Topic id="devices-tcp">**here**</Topic>.
:::
